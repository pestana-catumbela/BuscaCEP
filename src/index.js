import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';

const Root = ReactDOM.createRoot(document.getElementById('bc'));
Root.render(
  <React.StrictMode>
    <App></App>
  </React.StrictMode>
)
